<p id="notice"><%= notice %></p>

<h1>Find who is here</h1>

<% @people.each do |profile| %>
	<div class="entry">
	<p>
	<%= puts @user_id %>
	</p>
	<%= image_tag(profile.image_url) %>
	</div>
<% end %>